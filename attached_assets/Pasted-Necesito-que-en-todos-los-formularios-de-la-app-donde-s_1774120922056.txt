Necesito que en todos los formularios de la app donde se pide DNI, al escribir los 8 dígitos se autocomplete automáticamente el nombre y apellidos consultando RENIEC. Lee todos los archivos antes de modificarlos.
Los formularios donde aplica esto son:

Contactos de confianza — artifacts/legado/src/pages/trusted-contacts/index.tsx — campos fullName y dni
Registro de usuario — artifacts/legado/src/pages/auth/register.tsx — campos fullName y dni
Perfil del usuario — artifacts/legado/src/pages/profile/index.tsx — campos fullName y dni
Reporte de muerte público — artifacts/legado/src/pages/report-death/index.tsx — el campo DNI del reportante


COMPORTAMIENTO ESPERADO en todos los formularios:
Cuando el usuario termina de escribir exactamente 8 dígitos en el campo DNI, automáticamente y sin necesidad de hacer click en ningún botón:

Aparece un spinner pequeño al lado del campo indicando "Consultando RENIEC..."
Se llama a POST /api/trusted-contacts/verify-dni con el DNI
Si la respuesta es exitosa:

Se autocompletan automáticamente los campos de nombre con data.fullName
Aparece un badge verde debajo: "✓ Verificado en RENIEC: [NOMBRE]"
Los campos de nombre quedan editables por si el usuario quiere ajustar algo


Si falla:

Aparece un badge rojo: "DNI no encontrado en RENIEC"
Los campos de nombre quedan vacíos para que el usuario los llene manualmente


Si el usuario borra el DNI o cambia los dígitos, el badge desaparece y los campos vuelven a estar vacíos


IMPLEMENTACIÓN — Crear un hook reutilizable
Crear archivo nuevo artifacts/legado/src/hooks/use-dni-lookup.ts:
typescriptimport { useState, useCallback } from "react";
import { getAuthHeaders } from "@/hooks/use-auth";

type DniResult = {
  status: "idle" | "loading" | "success" | "error";
  fullName?: string;
  firstName?: string;
  firstLastName?: string;
  secondLastName?: string;
  message?: string;
};

export function useDniLookup() {
  const [result, setResult] = useState<DniResult>({ status: "idle" });

  const lookup = useCallback(async (dni: string) => {
    if (!dni || dni.replace(/\D/g, "").length !== 8) {
      setResult({ status: "idle" });
      return null;
    }

    setResult({ status: "loading" });

    try {
      const res = await fetch("/api/trusted-contacts/verify-dni", {
        method: "POST",
        headers: {
          ...getAuthHeaders(),
          "Content-Type": "application/json",
        },
        body: JSON.stringify({ dni: dni.trim() }),
      });

      const data = await res.json();

      if (!res.ok) {
        setResult({ status: "error", message: data.error || "DNI no encontrado" });
        return null;
      }

      setResult({
        status: "success",
        fullName: data.fullName,
        firstName: data.firstName,
        firstLastName: data.firstLastName,
        secondLastName: data.secondLastName,
      });

      return data;
    } catch {
      setResult({ status: "error", message: "Error de conexión con RENIEC" });
      return null;
    }
  }, []);

  const reset = useCallback(() => {
    setResult({ status: "idle" });
  }, []);

  return { result, lookup, reset };
}

IMPLEMENTACIÓN — Crear un componente reutilizable DniInput
Crear archivo nuevo artifacts/legado/src/components/ui/dni-input.tsx:
typescriptimport { useEffect, useRef } from "react";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Loader2, BadgeCheck, AlertCircle, CheckCircle2 } from "lucide-react";
import { useDniLookup } from "@/hooks/use-dni-lookup";

type DniInputProps = {
  value: string;
  onChange: (dni: string) => void;
  onResolved: (data: { fullName: string; firstName: string; firstLastName: string; secondLastName: string }) => void;
  onClear?: () => void;
  label?: string;
  required?: boolean;
  className?: string;
};

export function DniInput({
  value,
  onChange,
  onResolved,
  onClear,
  label = "DNI",
  required = false,
  className = "",
}: DniInputProps) {
  const { result, lookup, reset } = useDniLookup();
  const debounceRef = useRef<ReturnType<typeof setTimeout> | null>(null);

  useEffect(() => {
    const digits = value.replace(/\D/g, "");

    if (digits.length !== 8) {
      reset();
      if (onClear) onClear();
      return;
    }

    // Debounce de 600ms para no llamar en cada tecla
    if (debounceRef.current) clearTimeout(debounceRef.current);
    debounceRef.current = setTimeout(async () => {
      const data = await lookup(digits);
      if (data) {
        onResolved({
          fullName: data.fullName,
          firstName: data.firstName,
          firstLastName: data.firstLastName,
          secondLastName: data.secondLastName,
        });
      }
    }, 600);

    return () => {
      if (debounceRef.current) clearTimeout(debounceRef.current);
    };
  }, [value]);

  return (
    <div className={`space-y-1.5 ${className}`}>
      <Label className="flex items-center gap-1.5 text-gray-700 text-sm">
        <BadgeCheck className="w-4 h-4 text-violet-500" />
        {label} {required && <span className="text-red-500">*</span>}
      </Label>

      <div className="relative">
        <Input
          value={value}
          onChange={(e) => {
            const digits = e.target.value.replace(/\D/g, "").slice(0, 8);
            onChange(digits);
          }}
          placeholder="12345678"
          maxLength={8}
          className="rounded-xl h-11 text-gray-900 tracking-widest font-mono pr-10"
          inputMode="numeric"
        />
        {result.status === "loading" && (
          <div className="absolute right-3 top-1/2 -translate-y-1/2">
            <Loader2 className="w-4 h-4 animate-spin text-violet-500" />
          </div>
        )}
        {result.status === "success" && (
          <div className="absolute right-3 top-1/2 -translate-y-1/2">
            <CheckCircle2 className="w-4 h-4 text-green-500" />
          </div>
        )}
        {result.status === "error" && (
          <div className="absolute right-3 top-1/2 -translate-y-1/2">
            <AlertCircle className="w-4 h-4 text-red-500" />
          </div>
        )}
      </div>

      {result.status === "loading" && (
        <p className="text-xs text-violet-600 flex items-center gap-1">
          <Loader2 className="w-3 h-3 animate-spin" />
          Consultando RENIEC...
        </p>
      )}

      {result.status === "success" && result.fullName && (
        <div className="flex items-center gap-2 px-3 py-2 rounded-xl bg-green-50 border border-green-200 text-sm">
          <CheckCircle2 className="w-4 h-4 text-green-600 shrink-0" />
          <span className="text-green-800">
            Verificado: <strong>{result.fullName}</strong>
          </span>
        </div>
      )}

      {result.status === "error" && (
        <div className="flex items-center gap-2 px-3 py-2 rounded-xl bg-red-50 border border-red-200 text-sm">
          <AlertCircle className="w-4 h-4 text-red-500 shrink-0" />
          <span className="text-red-700">{result.message}</span>
        </div>
      )}

      <p className="text-xs text-gray-400">
        Los nombres se completan automáticamente al ingresar los 8 dígitos
      </p>
    </div>
  );
}

PASO 3 — Aplicar DniInput en contactos de confianza
En artifacts/legado/src/pages/trusted-contacts/index.tsx, reemplazar el campo DNI actual por el componente DniInput:
tsximport { DniInput } from "@/components/ui/dni-input";

// Dentro del formulario, reemplazar el bloque del campo DNI por:
<DniInput
  value={form.watch("dni") || ""}
  onChange={(digits) => form.setValue("dni", digits)}
  onResolved={(data) => {
    if (!form.getValues("fullName")) {
      form.setValue("fullName", data.fullName);
    }
  }}
  onClear={() => {
    form.setValue("fullName", "");
  }}
  label="DNI del contacto"
  required
/>

PASO 4 — Aplicar DniInput en el registro de usuario
En artifacts/legado/src/pages/auth/register.tsx, buscar el campo de DNI si existe, o agregarlo antes del campo de nombre completo:
tsximport { DniInput } from "@/components/ui/dni-input";

// Agregar estado para el DNI
const [dni, setDni] = useState("");

// En el formulario agregar antes del campo de nombre:
<DniInput
  value={dni}
  onChange={setDni}
  onResolved={(data) => {
    form.setValue("fullName", data.fullName);
  }}
  label="Tu DNI peruano"
  required
/>
Si el schema de registro no tiene campo dni, agregarlo al schema Zod:
typescriptdni: z.string().length(8, "DNI debe tener 8 dígitos").optional(),
Y enviarlo al backend junto con los demás datos del registro.

PASO 5 — Aplicar DniInput en el perfil del usuario
En artifacts/legado/src/pages/profile/index.tsx, buscar el campo de DNI si existe y reemplazarlo por DniInput. Si no existe el campo DNI en el perfil, agregarlo:
tsximport { DniInput } from "@/components/ui/dni-input";

<DniInput
  value={form.watch("dni") || ""}
  onChange={(digits) => form.setValue("dni", digits)}
  onResolved={(data) => {
    form.setValue("fullName", data.fullName);
    form.setValue("displayName", data.fullName);
  }}
  label="Tu DNI"
/>

PASO 6 — Aplicar DniInput en el reporte de muerte público
En artifacts/legado/src/pages/report-death/index.tsx, el campo donde el reportante ingresa su propio DNI para identificarse debe usar DniInput:
tsximport { DniInput } from "@/components/ui/dni-input";

<DniInput
  value={reporterDni}
  onChange={setReporterDni}
  onResolved={(data) => {
    // Mostrar el nombre del reportante como confirmación
    setReporterName(data.fullName);
  }}
  label="Tu número de DNI"
  required
/>
Y mostrar debajo un mensaje de confirmación cuando se resuelve:
tsx{reporterName && (
  <p className="text-sm text-green-700 font-medium">
    Reportando como: <strong>{reporterName}</strong>
  </p>
)}

PASO 7 — Verificar que el endpoint existe
Verificar que en artifacts/api-server/src/routes/trusted-contacts.ts existe el endpoint POST /verify-dni que llama a https://api.decolecta.com/v1/reniec/dni?numero=... usando process.env.RENIEC_API_TOKEN. Si no existe crearlo con el código que se proporcionó en la implementación anterior.